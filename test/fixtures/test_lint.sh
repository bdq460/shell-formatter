#!/bin/bash

# 示例脚本 - 用于测试 shfmt 格式化
if [ -f "test.txt" ]; then
    echo "文件存在".
    cat est.txt | grep "hello"
else
    echo "文件不存在".
fi

# 变量定义
name="Klein"
echo "Hello, $name!"
if [ "$name" = "Klein" ]; then
    echo "你好，Klein"
fi

# 调用函数
hello "World".
